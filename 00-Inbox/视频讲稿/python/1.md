Python入门半小时，剩下靠AI
https://www.bilibili.com/video/BV1xHn9z8EPX/?spm_id_from=333.337.search-card.all.click&vd_source=ee9d30f304bf8071b67bef309a4dabf6
王小二数据分析 2025-09-28 09:29:57

这个type函数呢是表示显示一下这一个变量 它的数据类型是什么 点击一下运行 那你会看到上面这边显示的是它的内容 你好事件 然后下边这边有一个叫class st star 是dream的缩写 就是字符串 Python里面的变量有一定的命名规则 它是区分大小写的 这两个变量是不一样的 message第一个是小写 然后第二个大写 这两个变量是属于不一样的变量 然后变量里面不能包含中文 以及开头不能有数字 比如说message就是前面写一个一 它就不能作为一个变量 这个编辑器都显示了报错 但是呢数字不能作为开头 但是中间包含数字是没有关系 这边写个一 然后我们都补全一个一运行一下 这样是运行成功的 下面介绍第二个数据类型 整形整形的话就是一个整数 比如说我们的年龄 然后我们也可以把它打印一下 在这边写个type edge运行一下 你看到后面这边多了一个18 然后这边多了一个class int 它表示的是int这个类int的话就是整数的意思 有整数的话就会有分数 分数的话就用浮点型来表示 

然后我们输入一下派 就是圆周率 同样的在后边这里补全一下 运行后面多了一个3.14159 然后这边多了一个class叫float 就是浮点型 最后一种呢叫布尔型 就是是或否true和false 然后或者叫零一 比如说我们做一个判断 叫is student把它命名为true 然后在后面加上一个意思 student这边加上一个type 运行一下 在最后面呢有一个true 然后这边有个class叫BO啊 确的话它是区分大小写 首字母以大写开头 然后后面都是小写 也不能写成这样子 这样子他是报错的 除了true之外 另外一个值就是false false的话写法也是大写开头 然后后面全小写运行 下面这边显示了false 介绍完基本的数据类型之后 我下面介绍一下列表 它可以把这几种基本的数据类型做一个组合 比如说我这边有一个变量叫fruit 就是水果是三种水果 分别是苹果 香蕉和橘子 每一个都用引号来引用 就表示它们都是字符串 然后我们要把它组合起来的话 就可以加上一个中括号 这样就显示成一个列表 列表里面的每一个值 比如说这个苹果呢它叫一个元素 然后它是有位置的 苹果这个位置在第零个位置 然后香蕉在第一个位置 然后橘子在第二个位置 计算机里面比较常用的就是从零开始计数 就是01234这种 然后有了这个位置之后 我们就可以引用这个元素 比如说我们想要看一下 

这个列表里面的第一个位置有什么fruits 然后引用一下零元素的引用 左边是我们的一个列表 然后后面加上一个中括号 里面包含的是我们这个元素的位置 然后我们引用的第一个元素它就是苹果 这样我们打印一下看看 然后在最下边这边就显示了一个苹果 然后你还可以写二二的话 就是对应这边的橘子 那么你运行一下 这边返回的就列表里面的元素是可以修改的 也就是说它可以进行重新的赋值 比如说我们想把第一个元素改成里子 那就可以这么写 这边引用的是第一个元素 也就苹果 然后这边是一个李子的字符串 然后中间的等号呢就是赋值 把这个列表第一个元素重新赋值成一个李子 然后在这边我们打印一下 第零个位置的元素是什么 它返回的结果就变成了里子 然后我们把它注释掉 这里顺便提一下Python里面的注释语句啊 比较常用的就是这样一个井号 然后有一个快捷键是CTRL加左斜杠号 然后你可以既可以注释它 也可以取消注释 这种符号在我们写代码的时候经常会用到 然后元素的内容也可以增加增 增加的话需要用到一个方法叫pd 我们想要增加一个元素叫西瓜 在后面加上一个西瓜 这样的话我们打印一整个列表 运行一下 最后增加了一个西瓜 这个列表本来是三个 然后经过append这个运算变成了四个 这边有个提醒呢 表示的是经过这个append的运算之后 

原来的列表已经发生了改变 这是编辑器本身都会给你的一个提醒 跟列表相对应的另外一个数据结构叫做元组 元组的话跟列表不一样的地方 就是它不能被修改 也也就是说它不能通过append 这种操作来增加一个元素 也不能通过赋值的方式来替换一个元素 第一个是列表 他通过中括号来引用啊 第二个是一个元组 它通过一个小括号来引用 然后我们把它分别打印一下 前面这里跟我们赋值的结果是一样的 然后在这边下面是两个的数据类型 列表的类型是那个list 然后原组的类型是table 然后在这边我们可以试着对它们进行 赋值的操作 这个编辑器已经对你做了一个提醒 说元组不支持赋值 如果你运行一下的话 它就会报错 这里提一下Python的报错语句 我们一般先看最后一行叫type error 然后它会这边会说明一下 你的报错的内容是什么 这边翻译过来就是原主对象 它是不支持赋值的 前面这边也有比较明细的报错信息 最后定位在这里 然后他已经帮你指示好了 这个操作是有问题的 现在报错呢 你在学Python的时候会经常遇到 你至少要知道一下它的结构是什么 大概讲的是什么意思 如果真的看不懂 你就直接抛给AI去问 下面介绍循环语句 刚才介绍列表呢有一个好处 就是它可以用来做循环遍历 循环语句在Python里面非常简单 for循环的语句呢就是for开头是for 

然后我们写一下循环变量for f in 它帮你自动补全了 然后我们打印一下f print f 我们先运行一下哈 后面的结果就是苹果 香蕉和橘子依次打印啊 for循环有两个关键词 一个是for 另外一个是in in 表示的是来自于哪一个数据结构 它可以是列表 也可以是元组 然后F的话表示的是这个列表里面的 每一个元素取一个临时的名字 然后管它叫F 这里的F呢可以你可以用任何的变量 比如说我就想管它叫fruit 这样运行一下也可以 如果你想用for循环来遍历数字的话 它还有一个比较常用的搭配叫range 这里的语法就是for i in range 值里面是我们想要输出的对象 比如说我就想输出一下I I就是range这里面的数字 然后range的话它是从零开始计数的 我们看一下结果是什么 它下面输出的是0123 所以range的话它是从零开始计数 这行代码的意思就是说range它是一个计数器 从零一直数到九 我用这for循环的话 把里面技术的每一个东西都给提取出来 在for循环内部这边有一个行为 我是把它直接打印出来 然后你也可以添加更复杂的行为 比如说我这边的I呢 就想计算一下它的平方是什么 然后在Python里面做平方计算的话是两个星一个二 然后我们运行一下 这边下面就输出了一个这个数字的平方 这边是0144 就是二的平方 然后九是三的平方 有了这个for循环之后 

你就可以对它做一些稍微复杂一点的计算 比如说我们想要做一个累加计算 我们这边有一个列表 它是从一一直到五 然后我要怎么算 它的总计就是1+2加3+4加五 这样求和求和的话 你也可以使用for循环 再用for循环求和的时候 你要指定一个初始值 比如说我这边就写一个叫total 它的初始值是零 基本思路 就是第二个抽取的数字跟前面一个数字相加 然后第三个抽取的数字 跟前面相加后的结果再相加 然后以此类推 一直加到最后一个 这样用for循环来写的话 就是for i total 然后有一个符号叫加等于I 加等于I的意思 就是说把计算后的结果赋值给这个total 重新复制给total 我们可以在后面print一下 你看第一次循环的时候 total变成一 然后第二次循环变成三 就是1+2 第三次循环结果是六 是1+2加三 然后面是十和15 最后输出的结果偷偷它变成了15 我们就可以通过这种方式来进行一个 累加的操作 下面来介绍if语句 它是用来做逻辑判断的 它的作用跟excel里面的if语句是一样的 比如说我我们用一个最简单的if语句 这边有个A组 我就想判断一下一个人的年龄是不是成年 我20岁 然后下面做一个判断 如果年龄小于18岁 那么我print一下未成年 然后其他情况下 也就是说年龄大于18岁 我就print一下成年 前面这里if呢写了一个判断的条件 如果edge这个变量小于18岁 

那么这个人就是未成年的 else这边呢他不需要写条件 他的意思就是说除了上面这种情况之外 其他的都是成年人 然后运行一下 因为我现在是20岁 所以这个条件不成立 然后我就会走else这条支线 输出的结果就是成年人 如果我把它改成16岁运行一下 这边结果就是未成年 我先判断这一条AH是否小于18岁 然后我符合 因为我只有16岁 然后输出的结果就是未成年 然后符合这条条件之后呢 他就不会再去做这个判断 那么我再加一个条件 比如说我大于65岁的时候就变成老年人了 这个时候要做第二个判断 这边中间要加上一个叫l if 我这边的年龄要小于65岁 这个时候我才是成年人 把它复制过来 然后其他情况下就是说我大于等于65岁 这个时候我是老年人啊 然后他的逻辑就是从上往下依次做判断 我先判断一下他是不是小等于18岁 如果不是再走下一个判断 如果再不是最走下一个 然后我们看第二个这边的逻辑判断 age小于65岁 他没有加上一个条件 说AH大于等于18岁 这是因为呢如果edge条件已经满 是小于18岁的话 他就会自动进行这个逻辑判断 就不会进入第二个逻辑判断 所以这里小于65岁的意思 其实是大于等于18岁和小于65岁的意思 我们这边写一个20岁运行一下 他是一个成年人 然后我们再写一个66岁运行一下 他是老年人 下面介绍一个非常重要的数据类型 

叫字体 它代表着一种映射关系 这是一个比较简单的字典形式 这样一行看起来可能会有点混乱 所以有些人比较喜欢这么写 这样形式看起来会清晰一点啊 第一个部分记录的是名字 第二个部分记录的是年龄 第三个部分记录的是一个城市 然后中间这每一个部分都用一个逗号隔开 这里的 然后冒号王小二合在一起呢 它是一个整体 中间这边一个冒号表示的是一个映射关系 用字典的术语来讲 它是一个键值对的关系 叫做key value 然后前面这边叫做key 它是一个键 然后后面这边是表示这个键对应的值叫做value 在互联网的世界里 这种键值对的数据结构非常常见 比如说一种非常典型的数据文件叫做JASON 它记录的就是这样一种键值对的关系 这边是一个比较典型的JASON文件 每个粉色的部分都是它的键 就key 然后后面每个绿色部分都是一个value 有了字典之后 你可以对它进行引用 比如说我想要知道一下person 这个人的名字是什么 你就可以直接这样引用person 然后name person 这是一个字典 然后name的话是它的一个键 然后我通过索引它的键可以得到它的值 我们看一下打印的结果是什么 下边这边返回的是王小二字典 有一系列的方法 比如说我想要得到字典所有的键 就可以通过kiss这种形式的表达 是在面向对象的编程语言里面非常常见 前面这边表示的是一个类 

然后后面表示的是类的方法 你可以简单理解为它就是一个函数 但是它函数不是写在前面 而是写在后面 专业术语叫做方法 通过调用这个方法 可以帮你完成一些类似于函数的任务 这里的key方法是字典这个类 它内置的一个方法表示的是返回这个字典 所有的键 我们运行一下 你看这边的结果叫做dix key 然后里面是一个列表 然后你还可以得到这个字典的value 就是它的值 这边运行一下 前面这边是它的T 后面这边是它的值 然后如果你想要得到所有的情况 就可以用item这个方法表示的是键跟值一起 你看到这里里面这一部分 它其实返回的就是一个列表 因为最外面一层是一个中括号 然后每个列表里面返回的是一个元组 它是一个包含元组的列表 最后这边的item方法呢 它有一个比较好用的地方 就是可以用来做for循环 比如说我们这边写一下for t v in person 然后print这边跟我们之前学过的for循环 非常相似 但是有一个不一样的地方 就是for循环前面这边有两个元素 一个是叫K 一个叫V有key和value 后面的person用的一个item的方法来提取出来 上面这条注释的结果 就是我们item方法所返回的结果 因为这个元组有两个元素 这边的key对应的就是这边的内 然后这边的V对应的这就是这边的王小二 我们来运行一下 看一下结果 

这种方式它就会把里面的key和value都给打印出来 前面介绍了一些基本的数据结构 然后以此为基础介绍了列表字典 下面我们来介绍一个非常重要的部分 叫做函数 如果你经常使用excel呢 对函数肯定不陌生 函数作用就是做一些计算 然后Python这边所定义的函数呢也是做一些计算 它的基本逻辑就是我需要一个输入 然后我会有一个输出 然后中间的计算方式呢 excel里面是把它封装起来的 然后Python里面你有时候可能需要自己去定义 怎么去做这个计算 我们先来写一个最基本的函数 比如说我就想弄一个hello 然后它的结果是什么呢 我return hello world 我们来看一下这个函数的基本结构 前面是一个DEEF叫define 我要定义一个函数 hello是函数的名称 后面需要加一个括号 跟excel里面的函数是一样的 在括号里面是要加参数的 即使你没有任何参数 也要加上这样一个括号 表示我是一个函数 然后后面是一个冒号 冒号之后呢是函数的主体内容 然后return表示的就是我的返回值是什么 然后这边的返回值呢是一个固定的字符串 就是hello world 然后我把它打印一下 Print 定义这个函数之后 我就可以去调用它 然后在print里面写一个hello 调用函数的时候 你是需要把函数的名称 然后以及这两个括号给加上去的 然后有时候如果这个函数有参数的话 

你还要指定参数 然后我们运行一下 它返回的结果就是hello world 然后这时候如果我想要让它更动态一点 比如说这边的word我想让用户自己输入 然后这边的话用法就需要去改一下 然后这时候呢我需要加上一个参数 叫做name 就说name是让用户来输入的 然后在这里面呢也需要改一下 我需要加上一个hello 然后在后面加上一个变量啊 这边是name啊 这边不加引号 它表示的是一个参数 然后这个参数呢需要用户来自己去传 我现在没有调用函数的时候 我不知道这个参数是什么 我们在这边调用的时候 他这个编辑器做了一个提醒 上面有一个破折号 就表示说你没有传参 没有传参的话是没有结果的 我们运行一下 它会报错 就是说他这边的报错 结果就是它需要有一个参数叫做name 然后我们指定一下这个name 比如说我们就是玩小二能运行一下 结果下面的返回结果就是hero王小二 然后这边的name你还可以指定为其他的 比如说Python就变成了hello Python 这样就比较有用了 就是你可以通过传参的方式来修改 你函数的内容 另外有一种写法呢 是Python里面经常会使使用的叫F字符串 在这个字符串前面加上一个F 然后在里面加上我们的变量 这种写法跟刚才会很不一样 因为刚才的这边的name刚才已经说过了 不能放在引号里面 就是说不能变成一个字符串 

但是现在我这边这个内呢加上了一个大括号 大括号呢在前面再加上一个 表示的是对这个字符串做了一个格式化 然后里面就可以包含这个变量 这是一种比较特殊的写法 但是在Python里面非常常用 也非常好用 然后我们再来运行一下 看看结果是什么 那结果就变成了hello Python 然后我们把它改一下 比如说王小二运行一下 它变成hello王小二了 跟刚才的结果是一模一样的 这样写法就很优雅 非常Python 如果函数只能做这种简单的打印的话 那可能有点无聊 我们可以做一些计算 比如说我们定义一个函数 对它做一些 比如说我们刚才的平方值 然后我们print一下square 比如说五五的平方就是25 那返回结果就说 然后你还可以做更复杂的计算 X的X次方就是五的五次方 3125 我们还可以算更大的数值 比如说100的100次方是多少 哼哼是不是有点超乎想象 数值很大的 然后函数还有另外一个好处 就是可以把其他的数据类型都包含进来 就刚才我们讲过的字典也好 列表也好 都包含在函数里面 这样组合起来它们功能就比较强大 比如说我们刚才介绍过的一个for循环 对这样一个列表进行求和 它得到的结果是15 这种过程如果比较常用 就可以写到一个函数里面 比如说我们把它修改一下它的这个函数名字 那就赶它叫susn number 在前面这里把它都给删除掉 

最后呢我们print一下 我们看一下运行结果 它的结果也是15 跟刚才是一模一样的 在写成函数的好处 就是你后面要用的时候就可以直接去调 函数里面所定义的变量都是在函数内部起作用 在函数外部的话它是不起作用的 比如说这边有一个TOTO 它等于零是在函数内部定义的 如果我在函数外部去引用的话 这编辑器已经提醒你了 有一个红线就表示比较严重的提醒 运行一下你是会报错的 它表示的是这个total它没有被定义 这边的total呢是在函数内部定义 它在全局 就这脚本所在的全局 它是没有这个total的定义的 所以你在运行的时候就会报错 下面要介绍面向对象OOP 面向对象编程了一个比较核心的概念 就叫做类 在Python里面写类呢语法是这样子的 这边有一个关键词叫class 然后class后面跟上我们的类名 那类的定义呢遵循一样的规则 你不能用中文 然后开头不能有数字 一般情况下我们定义类的开头的时候 手写字母都是用大写的 用它来跟函数做一些区分 需要注意的是 类的后面是不需要跟上一个小括号的 因为加上小括号 你就是一个函数了 然后在类里面可以写很多的方法 这里我们就写了三个方法 那我们来看第二个方法sun 然后它的定义是跟我们刚才学过的函数 是一样的 有一个差别 就是这边加了一个self 其他的部分都是一模一样的 所以对于方法来说 

你可以简单理解为它就是一个函数 然后第一部分的方法呢叫做INIT 它是定义了一个初始值 然后这是一个必须的步骤 然后在这边的number表示的是 这个类必须要传一个参数叫做number 然后我们第二个方法叫SN 就是做一个加总 然后第三个方法叫average 就是求一下平均值 它的计算方法就是加总值除以它的长度 然后定义完这个类之后呢 我们就可以去引用它 比如说我们想要解决一下 刚才我们的用函数解决的问题 就是做一个加总 以求一个平均值 这时候我们这边有一个列表 使用类的时候 第一个步骤是要初始化这个类 然后我们初始化一下CALC在右边 这里就是我们的类 然后在类里面呢我们要传入一个参数 这个参数是我们在定义it的时候 要求必须要传入的啊 这边就必须写上 右边就定义了一个类 把它赋值给左边啊 左边这个东西呢就是一个类的实例 定义完成之后 你就可以引用这个类这个类里面的所有方法 比如说这边的sun这边的average 你就可以直接去调用 比如说我们想要算一下它的加总 我们可以print一下GLC它的sun方法 前面这边是一个类的实例啊 就是我们这边复制过的 然后后面这边是这个类的方法啊 这类的方法我们在这类本身去定义过了 然后因为这边我们已经传了参数 然后在这边的sun的时候就不需要再传入了 

然后运行一下会看到下面返回的结果 跟我们刚才用函数计算的是一样的 都是15 我们在使用一个方法叫就是下面的average average是在下面这边定义 然后它的定义方法就是用加总值除以 这个列表的长度就是五 加总值的话是15 然后列表长度是五 这两个除以下就是等于三 我们看一下结果 在下面这边就是一个3.0 面向对象呢是Python里面比较难学的一部分 我这边只做一些简单的介绍 不过好处是很多问题用for循环逻辑判断 或者是一个函数就可以解决 而面向对象就不是必须的 假如你在后面需要做一个大的系统 这时候就需要用到面向对象 这种情况下呢 我建议你还是更深入的去理解一下 你可以去看看书 或者去找一些视频看看 他们会做更详细的介绍 



--- 由 vCaptions 生成 ---