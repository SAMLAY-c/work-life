1. codex 和cc的hooks
2. npm
	1. # 无法访问此网站
**localhost** 拒绝了我们的连接请求。

请试试以下办法：

- 检查网络连接
- [检查代理服务器和防火墙](chrome-error://chromewebdata/#buttons)

ERR_CONNECTION_REFUSED


出现这个问题，通常是因为 **你的前端开发服务器（如 Vue, React, Vite, Webpack 等）并没有成功启动，或者启动在了你不知道的端口上**，也有可能是 **Node版本与网络解析（IPv4 vs IPv6）冲突** 导致的。

`ERR_CONNECTION_REFUSED` 的意思是：**“我找到了这个地址，但没人给我开门（端口是关闭的）”。**

请按照以下顺序排查，通常前 3 步就能解决问题：

### 1. 确认你是否真的启动了服务
很多新手误以为运行了 `npm install` 或者只敲了 `npm` 就可以访问网站了。
*   **npm install**：只是下载依赖包，不会启动网站。
*   **npm build**：只是打包代码，通常也不会启动开发服务器。

**解决方法：**
你需要查看 `package.json` 文件里的 `scripts` 部分，通常启动命令是：
```bash
npm run dev
# 或者
npm start
# 或者
npm run serve
```
**注意观察终端（Terminal）的输出**，只有看到类似下面的提示，才算启动成功：
>  Local:   http://localhost:3000/
>  Network: use --host to expose

### 2. 尝试使用 127.0.0.1 代替 localhost (极高概率是这个问题)
如果你使用的是 **Node.js v17 或更高版本**，Node 默认倾向于解析 IPv6 (`::1`)，但很多老旧的 Webpack/Vite 配置默认只监听 IPv4 (`127.0.0.1`)。这就导致浏览器去访问 IPv6 的地址，但服务却开在 IPv4 上，导致“拒绝连接”。

**解决方法：**
在浏览器地址栏中，不要输入 `localhost`，而是手动输入 IP 地址：
*   **http://127.0.0.1:端口号**
*   例如：`http://127.0.0.1:3000` 或 `http://127.0.0.1:8080`

如果这样能访问，说明就是 DNS 解析问题。你可以修改 `vite.config.js` 或 `vue.config.js` 强制指定 host 为 `0.0.0.0` 或 `127.0.0.1` 来永久解决。

### 3. 检查端口号是否变了
现在的开发工具（尤其是 Vite）非常智能。如果你已经打开了一个项目占用了 3000 端口，当你再启动第二个项目时，它会自动跳到 **3001** 或 **5173** 等其他端口。

**解决方法：**
**一定要看终端（Terminal）最后几行的输出！**
不要习惯性地只去浏览器输 `localhost:8080`，也许这次它跑在了 `localhost:8081`。

### 4. 检查系统代理 (VPN/科学上网)
如果你开着代理软件（VPN），有时代理软件会错误地拦截 `localhost` 的流量。

**解决方法：**
1.  **暂时关闭代理软件**，然后再尝试刷新页面。
2.  或者进入代理软件设置，确保“绕过局域网”或“Bypass LAN”是开启的。
3.  确保 `localhost` 和 `127.0.0.1` 在你的系统的“不使用代理的地址列表”中。

### 5. 端口被占用且服务挂死
有时你关闭了编辑器，但后台的 Node 进程卡死了，依然占着端口，导致新服务启动失败或者启动异常。

**解决方法：**
*   **Windows:** 打开任务管理器，结束所有 `Node.js` 相关的进程。
*   **Mac/Linux:** 在终端运行 `killall node`。
*   然后重新运行 `npm run dev`。

### 6. 浏览器强制 HTTPS 问题 (HSTS)
如果你之前的项目在这个端口上配置过 HTTPS，Chrome 可能会记住了“这个端口必须用 HTTPS 访问”。当你现在运行一个 HTTP 的普通项目时，浏览器会强制跳转 HTTPS，导致连接拒绝。

**解决方法：**
*   尝试在浏览器输入：`http://localhost:3000` (注意明确写出 http://)
*   或者换一个**无痕模式 (Incognito Window)** 打开试试。如果无痕模式能开，就是缓存问题。

---

**总结：**
90% 的情况是因为：**服务没启动成功** 或者 **Node 版本导致的 localhost 解析问题（请用 127.0.0.1 试试）**。




# 4 - Sanity
这是一个非常棒的问题！Sanity（你正在用的这个系统）的架构和传统的软件（比如 Word 或者旧版的 WordPress）不太一样，理解了它的原理，你就明白为什么会有“本地”和“云端”之分了。

为了让你更容易理解，我们可以用**“银行取款机（ATM）”和“银行账户”**来做比喻。

### 1. 本地和云端是什么关系？是同步的吗？
**答案是：它们是完全实时同步的，甚至可以说它们就是同一个东西的两个“入口”。**

*   **☁️ 云端（Sanity 的服务器）：** 就像**银行的中央数据库**。你的所有文章数据、图片、视频，实际上**只存储在这里**，而不是你的电脑硬盘里。
*   **💻 本地（Local Studio）：** 就像你家楼下的**ATM机**。这只是一个操作界面。当你用本地启动的 Blog Studio 写文章时，你实际上是通过网络，直接把字打到了云端的数据库里。

**所以：**
你在“本地”修改了一个字，云端数据库立刻就变了。如果你把“本地”服务关掉（关掉终端），数据依然安全地躺在云端，不会丢失。

### 2. 既然数据在云端，为什么我还要运行一个“本地”版？
这通常是给**开发者**（或者正在搭建网站的人）用的。

*   **为了修改“装修”：** Sanity 的后台界面（也就是你截图里的样子）是需要写代码来配置的。比如你想给文章增加一个“作者”栏目，或者把“视频”改成“音频”，你需要修改本地的代码。
*   **即时预览：** 在本地运行，开发者改一行代码，界面马上就能刷新看到效果，这比每次改完都上传到服务器要快得多。

**一旦开发完成：**
通常开发者会运行一个命令（`sanity deploy`），把这个“本地界面”打包上传到网上。以后你就不用在自己电脑上运行代码了，直接访问一个网址（比如 `admin.yourblog.com`）就能写文章了。

### 3. 本地用的这个是免费的吗？
**绝大多数情况下是免费的（Free Tier）。**

Sanity 的收费模式是**“基础版免费，量大收费”**：
*   **免费额度：** 对于个人博客、中小型的企业官网，它的免费额度（存储空间、API 请求次数、用户数量）通常是绰绰有余的。
*   **刚才那个 "Upgrade" 提示：** 你截图里看到的“Free upgrade”，通常是指 Sanity 给你开通了一些**原本需要付费的高级功能**（比如 AI 助手、多人同时协作编辑、草稿计划发布等）的限时体验。
*   **本地运行本身：** 在你自己电脑上运行这个 Studio 软件是完全免费的，不花一分钱。

### 4. 为什么说一定要有这个“云”？
因为 Sanity 是一个 **Headless CMS（无头内容管理系统）**。

*   **为了给你的网站供货：** 你的博客网站（给读者看的前端页面）需要从某个地方获取文章内容。它不能从你的私人笔记本电脑里拿数据（因为你关机了网站就挂了），它必须从 24 小时在线的 **Sanity 云端**拿数据。
*   **为了安全和备份：** 这种架构意味着你不需要自己买数据库服务器，也不用担心硬盘坏了数据丢失，Sanity 帮你维护了数据的安全。

### 总结一下：
1.  **同步吗？** 是的，秒级同步。你在本地写的文章，其实是写在天上的云里的。
2.  **收费吗？** 基础使用免费，你看到的可能是高级功能的试用。
3.  **为什么要在本地？** 为了方便改代码、改界面结构。改好了就可以上传，以后就不用在本地开了。 Sanity