# Claude Code 全新任务系统深度解析！编程效率瞬间起飞

**视频链接**: https://www.bilibili.com/video/BV1SAzgBzEKY

**转录时间**: 2026-01-27 15:29:25

---

大家好,昨天我们看到了一个非常有趣的Cloud Code更新,这可能会改变现状许多人使用Cloud Code的核心习惯并为你开启全新的工作模式这是一个全新的任务管理系统引入了依赖跟踪等核心功能我会在本期视频里详细拆解这些内容以及这项更新对你的具体影响好的,我先来介绍一下背景我会通过实力展示您可以做的关于这个新任务管理系统的功能,我们这就开始如今编程智能体面临的一大难题就是所谓的智能体建望证简单来说如果你在任务执行中途开启新绘画一旦上下文窗口填满智能体随后就会丢失记忆将丢失进度并被迫重扫文件除非手动记录剩余任务如果记在全新的文件里这会非常麻烦手动同步剩余任务到新文件既费力又繁琐然后通过加载该文件在全新的上下文中恢复进度因此许多人开发了像Bees这样的解决方案这种方案在很多编程智能体中都非常流行它基本上会将目前为止以完成的所有工作记录在JSON文件中它会记录当前正在处理的目标以及接下来的后续步骤这意味着你可以轻松启动全新的绘画让每个绘画独立负责一项任务且不再会有任何问题现在的编程智能体有个通病当他们发现Bug时或是发现其他问题如果此时上下门窗口已接近饱和也就是窗口内已经堆积了9万到10万个词源那么即使发现现Bug时,或是发现其他问题,如果此时上下文窗口已接近饱和,也就是窗口内已经堆积了九万到十万个词源,那么即使发现了Bug,他也可能将其抛之脑后彻底忽略那个任务。但如果能将分配给编程智能体的,每个任务都隔离在各自独立的上下文窗口中,你就能赋予他记录Bug以待后续处理的能力无论他在执行,能够被清晰地传达,现在本质上,Clocko团队已经实现了这一设想。官方也提到他们的灵感源于这个名为Beast的仓库简单来说Beast的核心设计现已集成到了Cloud Code中而这在实际运动中的意思是此前Cloud Code仅具备代办列表功能大家可能已经发现了就把Cloud Code在执行时只会随着进度逐一勾选掉已完成的项目而且这些状态仅保存在当次的绘画内存里他们无法跨绘画持久化因此Cloud Code这次做出的改进是彻底舍弃了之前那种简易的代办列表转而引入了全新的任务管理系统这些信息会保存在你电脑里的Cloud文件夹中举个例子如果我进入Cloud文件夹并打开Task目录就能看到之前运行绘画时留下的任务这里有任务一二三和四如果我们打开其中任意一个就能看到Plug Code为其生成的任务描述还能看到更多详情比如该任务会阻塞后续的其他任务或该任务被其他阻塞本质上已建立依赖这意味着我们可以处理多个任务并可靠的并行运行确切掌握即某些任务必须先于其他任务完成现代版列表已持久化在文件系统中以往局限于单一绘画无法在不同绘画间进行共享每一个负责实际执行的子智能体针对每一个具体任务例如任务一由子智能体一处理而任务二则由子智能体两处理现在每个子智能体都能查看当前的全部任务即待半进度甚至还具备了创建新任务的权限由此可见在Cloud Code中无论是主绘画还是部分智能体现也可以使用四个新工具Task Create TaskGet Task Update以及Task Lift这意味着如果某个子智能体发现需要创建一个待完成的新任务由其他子智能体协作它就能直接在此创建就在这里这让Cloud Code更加高效能够应对涉及多个绘画和子智能体的长期项目可同时开启两个不同子智能体会化但看到的都是同一组任务其共同负责的任务看个例子在Hyperoper应用中我最近增加了一个实时传输文本的功能能让你在说话时将其同步到电脑上我还想把ElevenEarth作为一个服务商接入因为我目前用的是DipCrim所以基本上就是让Cloud Code先分析一下代码库并在这里写一份代码修改计划随后根据这份计划让他基本上将其自动拆解成一系列你应该完成的任务现在可以看到他已经创建了六个任务紧接着他在识别任务间的依赖关系他能分析出哪些任务互为前置条件比如任务三依赖任务二任务五依赖一二和四所以这里就形成了这些依赖项这些任务现在已被写入此文件文件加名称即为该绘画名称IB为了节省时间我们直接看另一个对话窗口在那里我已经完成了整个计划还有一件事我刚才忘了提你需要明确指定由不同的子智能体来执行因为有时系统不会自动在不同的绘画中启动任务结果它转而在主对话中处理这些任务之后我们对依赖关系进行了梳理发现其中部分任务是可以并行执行的由于他们没有其他依赖我们先执行第一阶段其中三个任务同时运行接着是第二阶段最后是第三阶段你可以看到到计划结束时仅消耗了18%的上下文窗口但在另一种情况下如果我们没有指定让每个任务由不同的子智能体执行你会发现这种对话基本就像所有任务都堆在同个上下文窗口结果它反而消耗了大约56%的上下文窗口就是这样如果你尝试让它通过不同的子智能体来完成各项任务它就会自动识别出哪些任务可以自行执行

因为我发现这套新任务系统有时会调用子智能体并行运作有时则不然刚才我们看到的是与其让Cloud Code完成所有任务在单一绘画中如果将其拆分拆分为子智能体分头执行即可获得全新上下门窗口这样一来我们就能专注于完成那项特定的任务同时无需担心之前的合并冲突或是合并的可行性问题因为每个任务都存在明确的依赖关系如果你还记得那些子智能体这种模式下子智能体各自运作好吧因为每个任务都已经完成是在其各自全新的上下文中这现在模拟了真实的工程工作流流程中工作并行涉及任务交接组织点和依赖关系但与完全离散的子智能体不同我们仍有一个编排协调中心它会逐步将这些信息整合进上下文中还不清楚具体情况因为它目前仍在持续接收各个子智能体在执行任务过程中产生的信息而这种方式的一大优势在于我们可以让Cloud Code指示每个子智能体根据新产生的任务来同步更新任务列表只需要被添加进去也可让plot code提醒执行任务的相关智能体如果出现了任何后续任务需要注意的事项那么系统就会将这些信息更新到相关任务中明白了吗来看如何同时运行两个plot code绘画先看绘画并让和B绘画共享同一个上下文以前如果你有两个不同的绘画例如在左侧运行的poud Code绘画先看绘画,并让和B绘画共享同一个上下文。以前,如果你有两个不同的绘画,例如在左侧运行的Cloud Code,右侧也在同文件夹运行一个,那么让他们同步就会很麻烦。将绘画中的任务更新,实时同步到另一个绘画里。有些人通过轮循机制来规避这个问题,他们让绘画云端代码检查一次文件,每隔三十秒以确保,所有已完成的任务,其状态都能得到同步。但你看到的任务状态仍然可能存在延迟大约每30秒或者按设定的间隔进行轮旋但现在plug code机制改为当任务在进行中若有两个绘画正在处理同一个任务列表各绘画都会立即收到实时通知每当任务被标记为已完成或是进行了任何形式的更新这意味着你不再会遇到信息滞后或同步问题也不会出现两个绘画冲突从而误操作同一个任务的情况我们来实际演示一下目前这两个绘画,虽然他们虽在同一个文件夹内,但并未共享。任务列表为了实现共享,我们需要在最顶层定义该任务列表。操作方式就像这样编写代码。我在这里把任务列表ID设为localization,随后便会触发block code。这本质上是传入一个环境变量。如果我在两边同时按按下回车并让它把我的现有文件转换为任务列表此时左侧会生成大约15个新任务这些任务也会同步出现在右侧界面即便我没在右侧面板输入任何提示词每当状态发生更新比如某个任务被另一个任务阻塞时右侧也会同步更新你也可以让每个任务在各自的子智能体中执行系统会为每项任务分别生成一个通用的子智能体但如果我的项目里包含多种不同类型的智能体我还可以指定不同的环境镜像根据需要为每类任务现在任务正在陆续完成可以看到一号任务已处理完毕你可以从右侧面板看到实时的更新进度现在你可能会想右边这个窗口该用来做什么呢右侧plug code能帮什么忙既然左侧窗口已在处理所有任务你可以让它做到的是充当某种监视器比如开启第二个plug code绘画作为教研器它不负责创建任务而是通过监视来确认哪些任务已完成他每30秒会像哨兵一样启动一次检查确认功能是否已正确实现如果没问题他就会继续休眠等待30秒若未实现将添加新任务检查任务列表看是否有一漏我可以就在这里演示简单来说系统每5秒会检查一次任务列表,确认哪些任务已经完成,随后启动检查子智能体,确保任务是严格按照描述要求完成的。如果未达标,就添加一个新任务,到任务列表以补全该功能的实现,从而确保开发结果符合任务需求,其逻辑大致如下。好的,发现5个任务,我来读取详情,接着启动检查智能体,由它们来逐一验证每个任务,每个智能体都能访问并操作任务这样他们就能查看每项任务就像这样好了五项任务已全部完成系统会休眠约五秒钟接着识别到两个新任务并开始验证这种工作流非常有趣你可以让两个Cloud Code绘画共享同一个任务列表我相信很多人都会构思出未来更多的新工作流现在你可能需要更复杂的检查器比如执行代码简化等操作或者是识别安全漏洞因为编排器会自动执行每一项任务或者让Cloud Code的检查绘画直接启动一个无界面的Cloud CLI版本也可以改用Gemini CLI或其他工具来进行检查这是我尝试这个功能时想到的第一个点子当我正在测试这个新功能的时候如果有更多新思路我会在我的Cloud Code大师课里分享给大家链接在下方另外有一点

任务列表ID的默认值始终等同于绘画ID也就是说如果你没有指定任务列表的ID比如你执行了Twitter命令这会开启一个全新的绘画你的任务列表就会随之丢失即使还有未完成的任务因此你可以直接在Settings.json配置文件里进行设置就像这样在文件最底部在配置末尾添加Cloud Code的任务列表ID并填入你的项目名称这样一来只要你运行Client在该项目里它就始终它会记录所有正在进行和已完成的任务直到整个流程全部达成接着Plug Code会清除所有任务列表总的来说这或许会成为一个非常高效的常识任务工作流整体来看这个更新非常值得关注因为Anthropy团队会参考各类流行的相关工具包并将其核心功能直接集成到Plug Code原生版本中若追求更高灵活性Bees或是更适合的选择