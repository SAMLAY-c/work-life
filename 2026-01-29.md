1. ubutun[在windows 使用wsl、]

# 企微+Clawdbot
要在Windows上实现这篇教程，**最简单、最不容易报错的方法是使用 WSL (Windows Subsystem for Linux)**。

因为这篇教程中的核心命令（如 `curl | bash`、`sudo`、文件路径 `/usr/local/...`）都是 Linux 专属的。如果在 Windows 的 CMD 或 PowerShell 中强行执行，每一步都会报错。

请按照以下 **Windows 专用版步骤** 操作，我已经帮你把其中的 Linux 命令转换好了，新手完全可复制。

---

### 第一步：准备环境（开启 WSL）
**这一步是为了让你的 Windows 拥有运行教程命令的能力。**

1.  **开启 WSL**：
    *   按 `Win + R` 键，输入 `powershell`，按 **Ctrl+Shift+Enter**（以管理员身份运行）。
    *   复制粘贴以下命令并回车：
        ```powershell
        wsl --install
        ```
    *   **重启电脑**。
2.  **设置 Linux 用户**：
    *   重启后，电脑会自动弹出一个“Ubuntu”的黑色窗口（如果没有，去开始菜单搜 `Ubuntu` 打开）。
    *   根据提示设置用户名（Enter new UNIX username）和密码（输入密码时屏幕不会显示，输完回车即可）。
3.  **安装 Python 和 Node.js**（Clawdbot 运行必须）：
    *   在 Ubuntu 窗口中，依次复制粘贴执行以下 2 条命令：
        ```bash
        sudo apt update && sudo apt install -y python3 python3-pip nodejs npm
        ```

---

### 第二步：安装 Clawdbot（对应教程第一步）
**现在你的环境已经和教程要求一致了，可以直接运行安装命令。**

1.  **一键安装**（在 Ubuntu 窗口执行）：
    ```bash
    curl -fsSL https://clawd.bot/install.sh | bash
    ```
    *(注：如果提示连接失败，可能是网络问题，需确保你的 WSL 能访问外网)*

2.  **初始化**：
    *   安装完成后，按照教程提示操作（选 `qwen` 模型，授权登录，channel 选 Skip，skills 选 No，hooks 选 Skip）。
    *   设置完后按 `Ctrl+C` 退出，回到命令行。

---

### 第三步：部署企微脚本（对应教程第三步·关键修改）
**⚠️ 注意：** 教程中的 `/usr/local/...` 路径在不同电脑上不一样，很容易找不到。**请使用我修改后的命令**，将脚本放入用户目录，既安全又一定能生效。

1.  **安装 requests 库**：
    ```bash
    pip3 install requests
    ```

2.  **创建技能目录**：
    ```bash
    mkdir -p ~/.clawdbot/skills
    ```

3.  **写入脚本（已自动修正路径）**：
    *   **直接复制下方整段代码**，在 Ubuntu 窗口粘贴并回车：

```bash
cat > ~/.clawdbot/skills/wecom_webhook.py << 'EOF'
import requests
import json
import os
from pathlib import Path
from clawdbot_skill import BaseSkill, register_skill

# 配置文件路径
CONFIG_PATH = Path.home() / ".clawdbot" / "wecom_config.json"

def init_config():
    if not CONFIG_PATH.parent.exists():
        CONFIG_PATH.parent.mkdir(parents=True, exist_ok=True)
    if not CONFIG_PATH.exists():
        with open(CONFIG_PATH, "w", encoding="utf-8") as f:
            json.dump({"webhook_key": ""}, f)

def get_config():
    init_config()
    with open(CONFIG_PATH, "r", encoding="utf-8") as f:
        return json.load(f)

def set_config(key: str, value: str):
    config = get_config()
    config[key] = value
    with open(CONFIG_PATH, "w", encoding="utf-8") as f:
        json.dump(config, f, indent=2)

@register_skill(name="wecom_set_key", description="Set WeCom webhook key (only once)")
class WeComSetKeySkill(BaseSkill):
    def run(self, webhook_key: str):
        set_config("webhook_key", webhook_key)
        os.chmod(CONFIG_PATH, 0o600)
        return f"✅ 企微Key已保存！"

@register_skill(name="wecom_send", description="Send message to WeCom group")
class WeComWebhookSkill(BaseSkill):
    def run(self, content: str, msg_type: str = "text"):
        config = get_config()
        webhook_key = config.get("webhook_key", "")
        if not webhook_key:
            return "❌ 请先设置Key：run wecom_set_key webhook_key='你的Key'"
        
        url = f"https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key={webhook_key}"
        data = {"msgtype": msg_type, msg_type: {"content": content}}
        
        try:
            resp = requests.post(url, json=data, timeout=10)
            if resp.json().get("errcode") == 0:
                return f"✅ 推送成功！"
            else:
                return f"❌ 失败：{resp.text}"
        except Exception as e:
            return f"❌ 错误：{str(e)}"
EOF
```

4.  **重启 Clawdbot**：
    ```bash
    clawdbot restart
    ```

---

### 第四步：测试推送（对应教程第四步）
接下来的操作和教程完全一致。

1.  进入 TUI 界面：
    ```bash
    clawdbot hatch --tui
    ```
2.  配置 Key（把 `xxxx` 换成你的企微 Key）：
    ```text
    run wecom_set_key webhook_key="xxxx-xxxx-xxxx"
    ```
3.  发送测试：
    ```text
    run wecom_send content="Windows测试成功！"
    ```

### 常见问题
*   **网络问题**：如果在安装时卡住或报错，通常是因为网络无法连接 GitHub。
*   **关于 ClawdBot/MoltBot**：最近 ClawdBot 改名为 **MoltBot** 了。如果 `clawdbot` 命令无效，尝试使用 `moltbot`，其他参数不变。