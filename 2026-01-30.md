# 1.留痕安装
3.9.12微信版本过低，不让用留痕了

# 2.motbot -github 云服务安装



# 华为云登陆
15396631779


# 免费域名

### 📊 省流总结：我该选哪个？

| 平台名称 | 免费域名后缀 | Cloudflare托管 | 推荐指数 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| **DigitalPlat** | `qzz.io`, `dpdns.org` | ✅ 支持 | ⭐⭐⭐⭐⭐ | 注册需填虚拟地址，国内可能被墙 |
| **DnShe** | `de5.net`, `cc.cd`等 | ✅ 支持 | ⭐⭐⭐⭐⭐ | **评论区更新：**目前所有域名均已支持CF |
| **ClouDNS** | `cloud-ip.cc`等 | ❌ 不支持 | ⭐⭐⭐ | 注册需干净IP（非梯子），解析记录限制50条 |
| **ZoneABC** | `webn.cc`等 | ❌ 不支持 | ⭐⭐⭐ | 自带CF企业版接入（非直接托管），有积分制 |
| **DeSec** | `dedyn.io` | ❌ 暂不支持 | ⭐⭐ | 德国老牌，安全性高，但不能改NS |

### 🛠️ 详细注册与使用教程

#### 1. DigitalPlat (最推荐，功能最全)
*适合：需要HTTPS证书、想用CDN加速的用户。*

*   **网址：** `dash.domain.digitalplat.org`
*   **准备工作：** 一个邮箱、一个GitHub账号（用于验证）、虚拟身份生成器（Google搜 "Fake Person Generator"）。
*   **步骤：**
    1.  **注册账号：** 填写信息时，**Phone（电话）**和**Full Address（完整地址）**必须符合格式（建议直接复制虚拟身份生成器的美国/英国地址），否则报错。
    2.  **选择域名：** 登录后台 -> 点击 `Domain Registration`。
        *   **注意：** 只能选 `qzz.io` 或 `dpdns.org`（另外两个us.kg/xx.kg需要付费买密钥）。
    3.  **托管到Cloudflare (CF)：**
        *   打开Cloudflare官网，点击“添加站点”，输入你刚才想好的域名（例如 `test.qzz.io`）。
        *   CF会给你两个Nameserver地址（例如 `bob.ns.cloudflare.com`）。
    4.  **完成注册：** 回到DigitalPlat页面，填入你自定义的域名，并在Nameserver栏填入CF给你的那两个地址，点击 `Register`。
*   **续费：** 有效期360天，到期前180天内免费续费。

#### 2. DnShe (潜力股，更新快)
*适合：想要更多域名后缀选择的用户。*

*   **网址：** `my.dnshe.com`
*   **准备工作：** 邮箱即可。
*   **步骤：**
    1.  **注册：** 正常注册登录。
    2.  **申请：** 点击左侧 `Free Domains` -> `Register New Domain`。
    3.  **选择后缀：** 列表中选择 `de5.net` 或其他（评论区提示现在所有后缀如 `cc.cd`, `cn.mt` 等都支持CF了）。
    4.  **托管：** 注册成功后，在管理面板修改Nameserver为Cloudflare的地址即可（参考DigitalPlat的CF步骤）。
*   **福利：** 默认5个域名，邀请别人可扩展到10个。

#### 3. ClouDNS (老牌，但限制IP)
*适合：主要做纯DNS解析，不需要CDN防护的用户。*

*   **网址：** `cloudns.net`
*   **避坑：** **注册时必须关掉代理/VPN**，使用本地干净IP，否则无法注册。
*   **步骤：**
    1.  点击右上角 `Sign in` -> `Register`。
    2.  选择免费域名 `cloud-ip.cc` 或 `abrdns.org`。
    3.  进入面板添加A记录指向你的服务器IP。
*   **注意：** 目前已无法修改NS记录托管到CF（评论区确认）。

#### 4. ZoneABC (积分制，功能特殊)
*适合：喜欢折腾，想体验CF企业版SaaS接入的高级用户。*

*   **网址：** `zoneabc.net`
*   **玩法：** 
    *   普通二级域名免费。
    *   高级域名需要积分（完善资料+20分，邀请+10分）。
*   **特点：** 虽然不能把NS改到Cloudflare，但它官方宣称可以享受CF的企业版计划（通常是指通过CNAME接入的方式）。

#### 5. DeSec (极简主义)
*适合：只需要一个简单的动态DNS解析。*

*   **网址：** `desec.io`
*   **步骤：**
    1.  首页输入想要的域名（后缀固定为 `dedyn.io`），输入邮箱。
    2.  去邮箱查收邮件，点击重置密码链接。
    3.  重置密码后登录即可管理。
*   **限制：** 目前NS记录锁死，无法托管到其他平台。

---

### 💡 重点提示 (必读)

1.  **关于Cloudflare (CF)：**
    *   如果你是建站新手，强烈建议选择 **DigitalPlat** 或 **DnShe**。托管到CF后，你可以免费获得SSL证书（https前缀），还能隐藏你服务器的真实IP，防止被攻击。
2.  **网络环境：**
    *   文章提到 `qzz.io` 和 `dpdns.org` 据说在国内部分地区被墙（无法访问）。如果你是做面向国内的服务，请测试后再决定。
3.  **时效性：**
    *   免费域名随时可能跑路或更改规则（如ClouDNS以前支持CF现在不支持）。
    *   **不要在这些域名上搭建重要数据！** 仅作为测试使用。
4.  **评论区彩蛋：**
    *   作者提到有新的 `eu.cc` 域名活动，无需认证，这通常是另一个网站（可能是 `eu.org` 的仿盘或类似），如果有兴趣可以去搜一下相关关键词。

# 关联账号：the